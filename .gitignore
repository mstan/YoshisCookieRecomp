build/
*.nes
*.bin
*.sav
*.log
*.zip
.mcp.json
debug.ini
*.cdl
*.mlbl
*.cht
disasm_ref/
tmp_*.py

# nesrecomp/ is a Windows directory junction → F:/Projects/nesrecomp.
# Not tracked in this repo. Pinned SHA recorded in nesrecomp.pin and
# enforced at CMake configure time.
nesrecomp

# nestopia-core was a submodule for verify mode. Removed from CI builds
# (-DENABLE_NESTOPIA_ORACLE=OFF). Local dev builds can clone it manually.
nestopia-core/

# Local Ghidra project (per-developer reverse engineering workspace)
ghidra/
